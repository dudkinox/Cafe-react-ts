export default interface StoreModel {
  id: string;
  address: string;
  idstore: string;
  image: string;
  latitude: string;
  longitude: string;
  name: string;
  open: string;
  status: boolean;
  tel: string;
  total_review: number;
  website: string;
}

export interface ImageStore {
  image: string;
}
